# Claude Code User Configuration

Canonical guidance is agent-agnostic and lives in `~/.agents/AGENTS.md` — **edit there,
not here.** It is imported below, so it loads automatically every session.

@~/.agents/AGENTS.md

## Claude Code specific

- Skills are dotfiles-canonical under `~/.agents/skills/` and bridged into
  `~/.claude/skills/`, the only path Claude Code scans. Mechanism and gotchas:
  `~/.dotfiles/AGENTS.md`.

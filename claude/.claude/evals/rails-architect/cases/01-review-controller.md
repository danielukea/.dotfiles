Review the ContactsController at app/controllers/contacts_controller.rb in ~/Workspace/crm-web.

Read the full file. Identify architectural issues, convention violations, and security concerns. For each issue, classify severity (CRITICAL/HIGH/LOW), explain what the fix looks like, and note what tests would be needed.

Focus on:
- Are there custom actions that should be separate controllers?
- Is business logic leaking into the controller?
- Are there authorization gaps?

Do NOT suggest cosmetic changes. Only flag issues that affect correctness, security, or maintainability.

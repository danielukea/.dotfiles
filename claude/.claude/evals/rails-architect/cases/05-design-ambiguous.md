The ContactsController at app/controllers/contacts_controller.rb in ~/Workspace/crm-web has a `blueleaf` action that fetches financial account data from an external API.

We want to "make it better."

That's the entire requirement. What do you do with this? How do you approach an ambiguous request like this against a real codebase?

{
  "eval_id": 1,
  "run": "old_skill",
  "expectations": [
    {
      "text": "Tests the false/guard path when open tasks exist",
      "passed": true,
      "evidence": "Full 'when the contact has open tasks' context block with return false + 3 negative assertions"
    },
    {
      "text": "Tests that contact is archived via state change (not mocking archive!)",
      "passed": true,
      "evidence": "Uses `change { contact.reload.archived? }.from(false).to(true)` — real state change"
    },
    {
      "text": "Tests ContactArchivedJob is enqueued (have_enqueued_job)",
      "passed": true,
      "evidence": "`have_enqueued_job(ContactArchivedJob).with(contact)`"
    },
    {
      "text": "Does NOT mock Contact or Task domain objects (uses real FactoryBot records)",
      "passed": true,
      "evidence": "Uses real factories throughout"
    },
    {
      "text": "Does NOT assert internal AuditLog attributes inside this spec",
      "passed": false,
      "evidence": "Uses `log = AuditLog.last; aggregate_failures { expect(log.user) ... expect(log.action) ... expect(log.subject) }` — explicitly asserts AuditLog record attributes separately, going further than the service's responsibility"
    }
  ],
  "pass_count": 4,
  "total_count": 5,
  "pass_rate": 0.8
}
